# dependencies
node_modules/

# builds
build/
dist/*
!dist/packages/
dist/packages/*
!dist/packages/tokens/
dist/packages/tokens/*
!dist/packages/tokens/**
.out/
.nx/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env
.env.*

# tests/coverage
coverage/

# local MCP config (contains machine-specific paths)
.mcp.json

# Adobe Clean font files — Adobe-internal only, not for distribution
# Download from Marketing Hub and place in packages/fonts/
packages/fonts/*.woff2
packages/fonts/*.woff
packages/fonts/*.ttf
packages/fonts/*.otf

# misc
.DS_Store
*.tgz
!releases/*.tgz
.idea/

*storybook.log
storybook-static

docs/case-study/*

# Internal scratch / working files — not for public distribution
tmp/
JIRA-tickets-outline.md
screenshots/

# MCP server apps — not yet open-sourced; deploy separately
apps/figma-console-mcp/
apps/s2a-ds-mcp/

# AI tool workflow configs — machine/user-specific, not for contributors
# .claude/commands/ is tracked so slash commands are shared with the team
.claude/settings.json
.claude/settings.local.json
.claude/memory/
.codex/
.cursor/
.playwright-mcp/
.codex/
.cursor/
.playwright-mcp/
