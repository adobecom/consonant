# Story UI Setup Guide (archived)

> **This approach has been retired.** Story UI required an API key, added process overhead, and produced less accurate output than working directly inside the repo with Claude Code + the s2a-ds MCP.
>
> **See instead:** [`prototyping-setup.md`](./prototyping-setup.md) — the current prototyping workflow using `apps/prototyping/`.
