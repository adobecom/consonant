/**
 * S2A Grid Package
 *
 * CSS-only grid system. Import the CSS directly — no JS runtime needed.
 * Requires @adobecom/s2a-tokens to be loaded first for spacing variables.
 *
 * Usage:
 *   import '@adobecom/s2a-grid/css/grid.css';
 */

export {};
