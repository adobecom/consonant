# dependencies
node_modules/
apps/authoring-poc/public/media/
apps/authoring-poc/public/reports/
apps/storybook/stories/assets/homepage/video/
apps/authoring-poc/reports/

# builds
build/
dist/*
!dist/packages/
dist/packages/*
!dist/packages/tokens/
dist/packages/tokens/*
!dist/packages/tokens/**
.out/
.nx/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env
.env.*

# tests/coverage
coverage/


# Adobe Clean font files — Adobe-internal only, not for distribution
# Download from Marketing Hub and place in packages/fonts/
packages/fonts/*.woff2
packages/fonts/*.woff
packages/fonts/*.ttf
packages/fonts/*.otf

# misc
.DS_Store
*.tgz
!releases/*.tgz
!releases/components/*.tgz
.idea/

*storybook.log
storybook-static

docs/case-study/*

# Internal scratch / working files — not for public distribution
tmp/
JIRA-tickets-outline.md
screenshots/
charts/
jira-tickets/

# Portfolio case study cache — not for public distribution
.cache/

# AI tool workflow configs — skills and commands are tracked; machine-specific files are not
.claude/settings.local.json
.claude/memory/
.codex/rules/
.cursor/settings.json
.playwright-mcp/
.github/scripts/token-release/.pr-body.md
.github/scripts/token-release/.diff-summary.json

# visual-eval render output (regenerated by npm run eval:visual)
evals/visual/out/

# contract gate report (regenerated by npm run gate)
evals/gate/out/
packages/components/evidence/artifacts/

# Local-only working notes (roadmaps, personal plans) — never committed
docs/local/

# Reference material read by agents, not ours to redistribute — never committed
context/golden-dataset/

# spec-dictionary renditions (regenerated by npm run specs:export)
packages/specs/out/exports/

# spec-dictionary portability example output (regenerated by its run.mjs)
packages/specs/examples/minimal/out/
