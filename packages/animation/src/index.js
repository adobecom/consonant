/**
 * S2A Animation Package
 *
 * CSS-only scroll-driven animations and parallax effects.
 * Import the CSS directly — no JS runtime needed.
 * Requires @adobecom/s2a-tokens to be loaded first for spacing variables.
 *
 * Load conditionally — only include on pages that use parallax effects.
 * All animation declarations are wrapped in @supports (animation-timeline: view())
 * so browsers without scroll-driven animation support receive no broken styles.
 *
 * Usage:
 *   import '@adobecom/s2a-animation/css/animation.css';
 */

export {};
